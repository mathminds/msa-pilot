apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: ms2-db
  name: ms2-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: ms2-db
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: ms2-db
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: ms2
            - name: POSTGRES_PASSWORD
              value: password
            - name: POSTGRES_USER
              value: user
          image: postgres:latest
          name: ms2-db
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: ms2data
      restartPolicy: Always
      volumes:
        - name: ms2data
          persistentVolumeClaim:
            claimName: ms2data
